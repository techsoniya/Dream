# a string can be sliced to get parts of the string
# strings cant be changed , strings which exists are immutable

name ="soniya"
nameshort= name[0:2] # starts from index 0 all the way till 3 (excluding 3)
print(len(name))
print(nameshort)
character1 =name[1]
print(character1)